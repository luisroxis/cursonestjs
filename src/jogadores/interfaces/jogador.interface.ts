export interface Jogador {
  readonly _id: string;
  readonly phonenumber: string;
  readonly email: string;
  name: string;
  ranking: string;
  positionRanking: number;
  urlPicture: string;
}
